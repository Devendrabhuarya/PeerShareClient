const ACTION = {
    ROOM_JOIN: 'room:join',
    USER_JOIN: 'user:join',
    USER_CALL: 'user:call',
    INCOMING_CALL: 'incoming:call',
    CALL_ACCEPTED: 'call:accepted',
    SEND_STREAM: 'send:stream'
}
module.exports = ACTION;